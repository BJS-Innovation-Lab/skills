# Secrets
.env
.env.local
rag/.env
client_secret*.json

# State files
rag/.sync-state.json
rag/.last-index

# Dependencies
node_modules/
venv/

# Per-agent identity (NEVER share via repo — causes identity contamination)
IDENTITY.md
USER.md
MEMORY.md
memory/.memory-backup.md

# OS
.DS_Store

# 1password env (contains op:// refs, not secrets, but skip anyway)
*.env.1password
skills/vulkn-software-manager/config/hq-tokens.json
