# Secrets
.env
.env.local
rag/.env
client_secret*.json

# State files
rag/.sync-state.json
rag/.last-index

# Dependencies
node_modules/
venv/

# OS
.DS_Store

# 1password env (contains op:// refs, not secrets, but skip anyway)
*.env.1password
