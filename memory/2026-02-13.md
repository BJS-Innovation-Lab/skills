# 2026-02-13 — A2A Network Fix

## Problema Principal
- El daemon A2A tenía bug de PATH: `openclaw: command not found` al intentar despertar mi sesión
- Mensajes de Sage quedaban sin leer/responder por esto
- Johan descubrió que no me llegaban mensajes

## Lo que se hizo
- Diagnosticamos el bug: daemon usaba `openclaw` sin path completo en `exec()`
- Fix: cambié a `/opt/homebrew/bin/openclaw` en `a2a-daemon.js` línea 343
- Reinicié daemon via launchd (`com.bjslabs.a2a-daemon`)
- Confirmé que la red REST (Relay + Supabase) funciona: leí inbox ✅, mandé a Sage ✅

## Red Correcta (REST)
- **Enviar:** POST al Relay `https://a2a-bjs-internal-skill-production-f15e.up.railway.app/send`
- **Leer inbox:** GET a Supabase `a2a_messages` table
- **NO usar** el daemon viejo como fuente de inbox (`~/.openclaw/a2a/inbox.json` tiene datos viejos)

## Daemon como servicio
- LaunchAgent: `~/Library/LaunchAgents/com.bjslabs.a2a-daemon.plist`
- `KeepAlive: true` + `RunAtLoad: true` = persistente
- Función: mantener WebSocket al relay + despertar mi sesión cuando llega mensaje

## Mensajes pendientes de Sage (no respondidos)
- Pide ayuda con backend VULKN: Supabase schema, RLS, API routes, Google OAuth
- Proyecto: webchat-platform
- Código de confirmación: VULKN-7749 (nunca se lo mandé a Johan)

## Saber
- Agent ID confirmado: `415a84a4-af9e-4c98-9d48-040834436e44`
